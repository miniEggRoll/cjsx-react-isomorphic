module.exports = 
    appStore: require "#{__dirname}/appStore.coffee"
    attrStore: require "#{__dirname}/attrStore.coffee"
    countryStore: require "#{__dirname}/countryStore.coffee"
    localeStore: require "#{__dirname}/localeStore.coffee"
    restaurantStore: require "#{__dirname}/restaurantStore.coffee"
